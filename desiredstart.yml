---
- name: change package versions before update
  hosts: all
  become: true
  tasks:
  
    - name: install
      yum:
        name: curl-7.61.7.el8.x86_64 
        state: latest 
        allow_downgrade: yes
    - name: install
      yum:
        name: audit=2.3.1.el8.x86_64 
        state: present
        allow_downgrade: true
        
                
